spring:
  datasource:
    url: jdbc:mysql://localhost:3306/sfg_prod?useSSL=false
    username: sfg_prod_user
    password: mihai
    platform: mysql
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    database: mysql
    show-sql: false
